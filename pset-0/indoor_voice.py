# defining the main function
def main():
    # converting the input to lowercase and storing it in the voice variable
    voice = input().lower()
    
    # prints the input in lowercase
    print(voice)
  
# calling the main function  
main()